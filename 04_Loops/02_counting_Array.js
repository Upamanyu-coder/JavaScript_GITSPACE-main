/* Write a while loop that counts from 5 to 1 and Stores the numbers in an array named co
 */

let counttdown = [];
let i = 5;

while (i >= 1) {
  counttdown.push(i);
  i--;
}

console.log(counttdown);
